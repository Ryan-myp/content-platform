# 版本下限对齐 python 3.13：pydantic>=2.10 / aiohttp>=3.9.5 起才有 cp313 预编译 wheel，
# 旧版固定号在 py3.13 环境（Docker 构建/CI）无 wheel 会触发源码编译（缺 rust）导致失败。
# 下限与本机实测通过的版本一致（fastapi 0.139 / pydantic 2.13 / aiohttp 3.14）。
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.10.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9
httpx>=0.27.0
aiohttp>=3.9.5
requests>=2.31.0
jinja2>=3.1.2
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
slowapi>=0.1.9
pypinyin>=0.55.0
python-pptx>=1.0.0
pillow>=10.0.0
numpy>=1.26.0
edge-tts>=6.1.0
# 短剧/数字人视频字幕烧录：自带带 libass 的 ffmpeg 二进制（系统 ffmpeg 无 subtitles 滤镜时兜底）
imageio-ffmpeg>=0.5.0
