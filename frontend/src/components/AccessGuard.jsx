import React from 'react'

/**
 * 页面访问守卫（本地免费版）
 *
 * 内容创作独立版为本地免费运行（AI 费用走用户中转站 Key），
 * 无会员/付费墙概念，所有页面直接放行。
 * 保留组件签名是为了不修改 30+ 处路由调用。
 */
export default function AccessGuard({ children }) {
  return children
}
